# Ignore everything
/*

# ----------
# Exceptions

# This file
!.gitignore

# Explain what this setup is about
!README.md

# Git
!.gitconfig

# Zsh + Oh My Zsh!
!.zshrc
!/.zsh-custom

# --------------------------
# .config directory
# Don't ignore the directory itself (to later allow subdirectories to be tracked),
# but ignore everything inside the directory until overriden in the following lines
!/.config
/.config/*

# Neovim
!/.config/nvim
# Ignore spellfiles, they can be downloaded if necessary
/.config/nvim/spell/
/.config/nvim/lazy-lock.json

# Kitty
!/.config/kitty

# KeePassXC
!/.config/keepassxc
